import { NgModule } from '@angular/core';
import { Routes, RouterModule } from '@angular/router';

const routes: Routes = [];

@NgModule({
  imports: [RouterModule.${routingScope==='Child'?'forChild':'forRoot'}(routes)],
  exports: [RouterModule]
})
export class ${upperName}RoutingModule { }
